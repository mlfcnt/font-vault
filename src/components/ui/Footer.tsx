import React from "react";
import { Footer as MantineFooter } from "@mantine/core";

export const Footer = () => {
  return (
    <MantineFooter height={60} p="md">
      Application footer
    </MantineFooter>
  );
};
